//
// AdminAlertRule.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Admin alert rule with customer info */
public struct AdminAlertRule: Codable, JSONEncodable, Hashable {

    public enum Condition: String, Codable, CaseIterable {
        case failureRate = "failure_rate"
        case latency = "latency"
        case consecutiveFailures = "consecutive_failures"
    }
    public enum Channels: String, Codable, CaseIterable {
        case slack = "slack"
        case email = "email"
        case webhook = "webhook"
    }
    public var id: UUID
    public var customerId: UUID?
    public var customerEmail: String?
    public var name: String
    public var condition: Condition
    public var threshold: Int
    public var channels: [Channels]
    public var isActive: Bool
    public var createdAt: Date

    public init(id: UUID, customerId: UUID? = nil, customerEmail: String? = nil, name: String, condition: Condition, threshold: Int, channels: [Channels], isActive: Bool, createdAt: Date) {
        self.id = id
        self.customerId = customerId
        self.customerEmail = customerEmail
        self.name = name
        self.condition = condition
        self.threshold = threshold
        self.channels = channels
        self.isActive = isActive
        self.createdAt = createdAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case customerId = "customer_id"
        case customerEmail = "customer_email"
        case name
        case condition
        case threshold
        case channels
        case isActive = "is_active"
        case createdAt = "created_at"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encodeIfPresent(customerId, forKey: .customerId)
        try container.encodeIfPresent(customerEmail, forKey: .customerEmail)
        try container.encode(name, forKey: .name)
        try container.encode(condition, forKey: .condition)
        try container.encode(threshold, forKey: .threshold)
        try container.encode(channels, forKey: .channels)
        try container.encode(isActive, forKey: .isActive)
        try container.encode(createdAt, forKey: .createdAt)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension AdminAlertRule: Identifiable {}
