//
// AdminCreateAlertRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Create a platform alert rule (admin) */
public struct AdminCreateAlertRequest: Codable, JSONEncodable, Hashable {

    public enum Condition: String, Codable, CaseIterable {
        case failureRate = "failure_rate"
        case latency = "latency"
        case consecutiveFailures = "consecutive_failures"
    }
    public enum Channels: String, Codable, CaseIterable {
        case slack = "slack"
        case email = "email"
        case webhook = "webhook"
    }
    public var customerId: UUID?
    public var name: String
    public var condition: Condition
    public var threshold: Int
    public var channels: [Channels]

    public init(customerId: UUID? = nil, name: String, condition: Condition, threshold: Int, channels: [Channels]) {
        self.customerId = customerId
        self.name = name
        self.condition = condition
        self.threshold = threshold
        self.channels = channels
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case customerId = "customer_id"
        case name
        case condition
        case threshold
        case channels
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(customerId, forKey: .customerId)
        try container.encode(name, forKey: .name)
        try container.encode(condition, forKey: .condition)
        try container.encode(threshold, forKey: .threshold)
        try container.encode(channels, forKey: .channels)
    }
}

